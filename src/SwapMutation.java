public class SwapMutation implements MutationStrategy {
    @Override
    public void applyMutation(Population population) {
        // Implement swap mutation on chromosomes in the population
    }
}
