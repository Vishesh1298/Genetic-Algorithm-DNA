public class TournamentSelection implements SelectionStrategy {
    @Override
    public void applySelection(Population population) {
        // Implement selection using the Tournament method
        // Adjust the fitness values of chromosomes in the population
    }
}
